# Reverie Evals

No live eval run has completed yet in this workspace.

The backend exposes:

- `POST /api/evals/run` to generate a reproducible demo eval result.
- `GET /api/evals/results` for the frontend charts.

When a DashScope key is available, replace the deterministic mock run with a live run and record the real numbers here.
